{
  "name": "pageqa",
  "version": "0.9.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klren0312/pageqa.git"
  },
  "description": "用自然语言驱动的页面 QA agent：pi-agent-core 编排测试步骤，browserskill(bsk) 驱动真实浏览器执行，LLM 端点可配置（OpenAI 兼容）；输出文本/JSON 报告与可直接接入 CI 的退出码。",
  "type": "module",
  "bin": {
    "pageqa": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "pretest": "npm run build",
    "test": "node --test tests/smoke.test.mjs",
    "test:unit": "npm run build && node --test tests/report.test.mjs tests/report-html.test.mjs tests/side-outputs.test.mjs tests/overlay-frame.test.mjs tests/replay.test.mjs tests/snapshot.test.mjs tests/tui.test.mjs tests/agent.test.mjs tests/navigate-diagnosis.test.mjs tests/credentials.test.mjs tests/model-catalog.test.mjs",
    "prepublishOnly": "npm run build",
    "serve:smoke": "serve -l 18888 examples"
  },
  "files": [
    "dist",
    "README.md",
    "examples"
  ],
  "keywords": [
    "page-qa",
    "e2e",
    "testing",
    "agent",
    "pi-agent-core",
    "browserskill",
    "bsk"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.86.1",
    "@earendil-works/pi-ai": "^0.86.1",
    "@earendil-works/pi-tui": "^0.87.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "serve": "^14.2.0",
    "typescript": "^5.5.0"
  }
}
