# pageqa

用自然语言描述页面测试意图，由 LLM 驱动真实浏览器执行；跑通之后可以把操作序列固化成回放脚本，之后零模型重跑同一条用例。

## Language

**用例（Case）**：
一段用自然语言描述页面测试意图的文本，按非空行拆成若干步骤。
_Avoid_：测试脚本、case 文件

**场景（Scenario）**：
用例中由 `## 标题` 分隔的独立运行单元，拥有独立的 bsk session 与浏览器窗口，也是回放的独立执行单元。
_Avoid_：测试集、suite 项

**场景来源（Scenario Origin）**：
场景进入运行队列的出处：某个用例文件，或用户在交互中的追加。
_Avoid_：用例来源、源用例、来源文件

**批处理模式（Batch Mode）**：
非交互的一次运行：stdout 只放最终报告，日志走 stderr，退出码交 CI。
_Avoid_：无头模式、headless、非交互模式

**交互模式（Interactive Mode）**：
终端直接运行时进入的界面：场景执行期间仍可追加场景，追加场景写回落点；退出后回到批处理模式的输出口径。
_Avoid_：TUI 模式、终端界面、REPL

**追加场景（Added Scenario）**：
交互模式运行期间由用户提交的新场景，与前置场景同等地参与运行、报告与回放脚本。
_Avoid_：新增用例、临时用例、补充用例

**落点（Write-back Target）**：
本次会话中追加场景写回的那个用例文件；没有落点时，追加场景只存在于本次会话。
_Avoid_：源文件、目标文件、输出文件

**运行队列（Run Queue）**：
尚未开始执行的场景序列；场景按提交顺序串行执行，轮到它时才创建自己的 bsk session。
_Avoid_：任务列表、todo 列表

**已取消（Cancelled）**：
场景的一种终态：用户在运行中主动中止，或从运行队列中移除。既不是通过也不是失败，不计入退出码，也不写入回放脚本。
_Avoid_：中断、中止、跳过

**用例步骤（Case Step）**：
用例中的一个非空行（`#`、`>` 行除外），运行前由 pageqa 统一编号为「第 k 步」。
_Avoid_：指令、命令行

**动作（Action）**：
一次浏览器操作（navigate / click / fill / upload / hover / scroll / wait），是回放脚本的最小执行单位。
_Avoid_：操作指令、tool call

**断言（Assertion）**：
对页面内容的校验，决定场景结论是 PASS 还是 FAIL。
_Avoid_：检查点、验证点

**录制（Recording）**：
一次带模型的运行中，把成功执行过的动作与断言记下来的过程。
_Avoid_：抓取、采集

**回放脚本（Replay Script）**：
从一次录制固化出来的、可零模型重跑的步骤序列。
_Avoid_：执行脚本、录制文件、trace、固化脚本

**回放（Replay）**：
按回放脚本逐步驱动浏览器，全程不调用任何大模型。
_Avoid_：重跑、执行脚本

**定位符（Locator）**：
元素的可复用语义描述：角色 + 可访问名 + 同名序号。
_Avoid_：选择器、xpath、ref

**看板带（Kanban Band）**：
交互模式里日志视口**上方**的那条常驻区域：把场景按状态分成 5 列（等待中 / 进行中 / 成功 / 失败 / 已取消，与 `ScenarioState` 一一对应），实时跟着运行队列变。给「总览」——每列只放得下的最近若干张，多了 `+N 更多`；全量列表仍在 `/status` 与日志里。纯展示、点击不做操作；终端列数低于 90 时不渲染，整片留给日志视口（见 ADR-0010）。
_Avoid_：状态栏、日志视口、运行队列
